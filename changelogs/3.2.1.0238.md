## 3.2.1.0238 (2025-06-04)

### 🐛 Fixes (6 changes)

- core: correction to version checking regex per znc on update checker
- correction to asset url grabbing on znc build/update package
- corrections to plex install and configuration process
> Specifically:
> - Modifies the installation process to use the plex user and group.
> - Allows setting an empty `software_install_directory` and handles the case where it is empty in configuration.
> - Fixes file ownership after installation.
- correction to source grabbing on Flaresolverr build/update package
- improve the reliability of Node.js version management in Overseerr build
- correction in UI to rtorrent feature-bind labeling causing management options to fail
